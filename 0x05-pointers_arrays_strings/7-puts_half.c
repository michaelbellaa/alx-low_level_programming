#include "main.h"

/**
 * puts_half - kdcybudvcf
 * @str: asbgcvyacdv
 * Description: nvciuvhrub
*/

void puts_half(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
		;

	i++;
	for (i /= 2; str[i] != '\0'; i++)
	{
		putchar('\n');
	}
}
